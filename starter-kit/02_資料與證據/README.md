# 02_資料與證據/

**用途**：存放研究的一手或二手資料與證據，例如訪談逐字稿、問卷資料、次級資料集、田野筆記、編碼簿等。

**讀寫紀律**：任務執行時唯讀。AI 會讀取本夾資料進行分析（如 thematic-analysis 編碼），但不會直接修改或覆蓋原始資料；分析後的編碼結果、主題地圖等新產出寫入 `05_輸出/`，經你確認後再視情況搬入本夾歸檔。

**檔名規則**：`專案名稱_內容類型_v1.副檔名`（如 `paper1_訪談逐字稿_v1.docx`）；同名檔案更新時遞增版號，保留舊版不覆蓋。
